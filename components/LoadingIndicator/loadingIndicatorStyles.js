import { StyleSheet } from "react-native";

export const loadingIndicatorStyles = StyleSheet.create({
    loaderWrap: {
        gap: 10,
    },
    loaderText: {
        fontFamily: 'Poppins',
        textAlign: 'center',
        fontSize: 12,
    },
})