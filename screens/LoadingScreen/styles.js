import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    loader: {
        width: 250,
        height: 250,
    },
    loadingText: {
        fontFamily: 'Poppins',
        marginTop: 10,
        letterSpacing: 1,
    },
})